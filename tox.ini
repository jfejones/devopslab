[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    pytest-cov
commands = pytest --cov=jfejones_devopslab --cov-report=xml --cov-config=tox.ini --cov-branch
 
[coverage:run]